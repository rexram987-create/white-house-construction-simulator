# Project Notes

Museum Edition v1 has been added as `home.html`.

The project currently includes:

- `index.html` — initial homepage shell
- `home.html` — Museum Edition v1 homepage
- `museum.css` — dark museum-style design
- `data/timeline-data.js` — historical timeline data
- `app.js` — interactive timeline behavior
- `bibliography.md` — initial source and image notes

Next recommended step: make `home.html` the main `index.html` page after checking the current `index.html` SHA.
